# Requirements

This directory is where you will place all requirements documents for the Ateneo application. The AI assistant will read these files to build out the interfaces.

Example file: `create-club-requirement.md`
